# Wave P — Jeff July-1 quick-fix batch (6 items)

## Context
Jeff's July-1 PDF (`From Jeff/gabriel-items-2026-07-01.pdf`) yielded 6 quick fixes, fully grilled 2026-07-02 (brainstorm + grill-with-docs + grill-me; all decisions locked). Two template-content fixes ride version bumps (published versions are DB-trigger immutable — seed edit → guarded re-seed appends DRAFT vN+1 → manual publish); two are code fixes; one is a verified no-change finding; one is a flag-gated email chrome change + a launch-time data edit. No Prisma migration. All stableKeys kept, so the locked QSP crosswalk, trends (scored-only), and longitudinal (ADR-0016) are all unaffected. Merges dark; launch same-session with each prod mutation individually authorized.

## Work items

1. **QSP v2 story-label differentiation (Jeff #3).** In `src/prisma/seed-qsp-v2-assessment.ts:274/282/290`, append " (Story 1 of 3)" / "(Story 2 of 3)" / "(Story 3 of 3)" to the three identical labels. stableKeys `P1_core_values_story_1/2/3` unchanged (Esperto-faithful 3 slots; crosswalk Q5a/b/c + imported answers depend on them — deletion rejected, deviation from Jeff's literal "remove one" already communicated in the Jul-2 report).

2. **LVA label fixes, one version bump (Jeff #14 + #17).** In `src/prisma/seed-lva-assessment.ts`: (a) "Leadership Team" → "Leadership team" in BOTH `FACTORS_FOR_MATRIX` (line ~112) and `FACTORS_FOR_CHECKBOX` (~131) — lockstep, sentence-case consistency with the other 13 factors; "Growth Financing" deliberately untouched (parked with #16's pending reword). Factor display labels only — `FACTOR_STABLE_KEYS` slugs unchanged. NOTE: `FACTORS_FOR_MATRIX` also derives the S5 "Why is … a hindrance?" question labels, so those change too — intended (same consistency fix), covered explicitly in the seed tests. (b) `S6_core_values` label (~line 590) → Jeff's copy: "What are your core values? If not yet set, what do you think the top 3 should be?" — stableKey kept (ADR-0001 "semantically identical" ruling: answers are free-text value lists either way; alignment across versions stays meaningful). Update the "verbatim from xlsx" seed comments to record both deliberate departures.

3. **LVA #18 — no code change.** Verified against the Esperto source workbook: all three priority questions (org / this-year / this-quarter) exist verbatim — a deliberate cascade, not a duplicate. Deliverable = the finding reported to Jeff at launch (Slack, terse).

4. **Coach-email name fallback (Jeff #5).** New shared helper module (e.g. `lib/assessments/respondent-display-name.ts`) exporting `displayName(first, last, email)` (trimmed name, else email — trim BEFORE truthiness testing: the OrgRespondent path currently yields `" "`, a truthy single space) and `greetingName(name)` (first token, "there" when blank OR when the value contains "@"). Both population sites use `displayName`: `report-email.ts:144` (publicTaker path) and `respondent-report.ts:286` (OrgRespondent path); `report-email.ts`'s local `firstName()` is replaced by `greetingName`. Because the `respondent-report.ts` fallback also feeds the on-screen `BrandedReport`/`QualitativeReport`, any greeting-style first-name derivation in those page renderers uses `greetingName` too — the respondent never reads their own email in a greeting/sign-off on ANY surface (email or page), while coach-facing subject/preface/cover show the address (Jeff's ask). Never a generic ("a responder"). Accepted leak surface: cover "Report for josh@x.com" — honest, not broken.

5. **wave-p-flags.ts.** Copy `wave-o-flags.ts` verbatim → `isInviteEmailChromeEnabled(opts?)` with `WAVE_P_INVITE_EMAIL_KILL/ENABLED/CANARY` (KILL > ENABLED > CANARY; call-time env reads; org-or-template-scoped canary; default OFF).

6. **Invite email chrome (Jeff #2.1 + #2.4), flag-gated.** In `invitation-email.ts`: new optional `coachLogoUrl` on `InvitationVars` + a larger CTA variant. Logo renders in the purple header beside the SU logo ONLY when the URL passes an email-specific **HTTPS-only** `safeImageSrc` gate (profileImage is free-text on two write paths; `http:` images are already stripped by the email sanitizer, so the logo gate matches — rejects `http:`, `javascript:`, bare filenames, empty; `escapeHtml` the src; used only as `<img src>`, never interpolated; fixed max-height so an oversized image can't blow up the 560px layout; `alt` = coach name). The `alt` (coach name) is user-controlled too: `escapeHtml` + control-strip it with the same helpers as every other insertion — tests cover quotes, angle brackets, control chars in coach names. Button: same "Start the assessment" label (relabel belongs to Jeff's pending #2.3 copy), larger padding/font. **Full custom-HTML override invitations (`renderFullHtmlBody` path) are explicitly EXCLUDED** — the coach authored the entire HTML, shell chrome doesn't apply; test asserts flag ON + `invitationBodyHtml` set = unchanged output. Keep the module pure: callers evaluate the flag and pass the variant/logo. **Accepted-with-audit risk (tracking pixel):** any https `profileImage` becomes a remote image loaded on respondent email-open — a third-party host learns opens. Values are written only by authenticated coaches/admins, and Wave K already renders the same remote images on web report pages; launch preflight audits every existing `Coach.profileImage` host (PII-free) before the flip. Proxy/host-allowlist hardening = recorded follow-on (write-side validation already out of scope).

7. **Wire the 4 send paths — the FULL chain, not just route selects.** Logo identity mirrors `resolveCoachName` exactly: `(creatorCoach ?? organization.owner)` — both are Coach rows (`ownerCoachId → Coach`), so name and logo always come from the SAME coach; an admin-created campaign shows the org-owning coach's name AND logo, never a name/logo mismatch. Add `profileImage` to the `creatorCoach` + `organization.owner` selects of: invite route, resend route, reminders route, Inngest fan-out (`assessment-invite-fanout.ts`); thread `coachLogoUrl` through `SendInvitesInput` / the `InviteMailer` contract / `sendAssessmentInvitationEmail` so it can't be dropped between route and mailer. No resolvable coach → no logo, email renders exactly as today. Flag evaluated per send with the campaign's org/template ids. Tests assert the MAILER receives `coachLogoUrl` (incl. `creatorCoach=null, owner present`) across all 4 paths. PII-free observability on each send: log `chromeVariant`, `logoIncluded`, `logoRejectedReason` (never the URL) alongside the existing send logging, so a silent logo fallback after the global flip is diagnosable.

8. **Coach name in body (Jeff #2.2) — launch-time data edit, no code.** Edit the QSP template row's `invitationBodyMarkdown` via the existing admin UI/API to weave in `{{coachName}}` (token already allowed in body + subject; resolves via `resolveCoachName(creatorCoach ?? organization.owner)` — same Coach as the logo, item 7 — so name and logo always agree; the "your coach" default only fires when NEITHER exists, which no real campaign hits since orgs always have an owner). LIVE for future sends of in-flight campaigns (accepted; per-campaign overrides untouched); mixed old/new wording across one campaign's resends is accepted cosmetic drift. Sequencing: runs BEFORE the QSP re-seed (see item 10, contentHash).

9. **Tests (TDD, subagent-driven).** (a) Seed content tests: new labels exact (incl. the derived S5 "Why is … a hindrance?" labels), stableKeys/sortOrders/isRequired unchanged, factor arrays lockstep. (b) Flag tests: mirror wave-o-flags suite. (c) Invitation email: flag-OFF byte-identical (inline-snapshot, fixed inputs — new precedent alongside the suite's substring style), flag-ON logo present/absent/https-only-rejections (`http:`, `javascript:`, bare filename, empty), button styles, escaped URL, no-resolvable-coach unchanged, full-HTML override unchanged, mailer-receives-`coachLogoUrl` across all 4 send paths incl. `creatorCoach=null/owner present`. (d) Name helpers: blank → email, whitespace-only (`" "`) → email, normal name untouched, email-value greeting → "there" in BOTH the email builder and the page renderers, coach subject shows address. (e) `CI=true npx next build --turbopack` green.

10. **Prod re-seed + publish sequencing (worry #1).** Launch preflight (read-only, authorized): (a) list ALL QSP + LVA versions and assert **zero `publishedAt IS NULL` rows exist** — not just "latest is published"; any lingering draft (old or new) = STOP and inspect before any FORCE; (b) audit every `Coach.profileImage` host (item 6's accepted-risk gate); (c) snapshot the current QSP `invitationBodyMarkdown` verbatim (rollback value). **QSP body edit (item 8) runs BEFORE the QSP re-seed** so the new draft's `contentHash` is computed over the final invitation body — publishing first and editing after would leave the published hash tied to the old body and generate a spurious extra draft on every future re-seed. The body edit has no CAS on the admin route, so harden procedurally: edit → **re-read the stored row and verify it equals the intended text verbatim** → only then seed; after seeding, verify the new draft's hash inputs include the final body. Then run both re-seeds (DRAFT-only, audited, advisory-locked — concurrent seed runs are serialized by `pg_try_advisory_xact_lock`) → verify each new draft's questions JSON (labels exact, stableKeys unchanged) and **record each draft's exact version id** → **dry-run the publish validation (`TemplateVersionForPublishSchema`) against BOTH drafts before publishing EITHER** → publish each via the admin publish route **targeting the recorded draft id specifically** (the route accepts any draft — never publish "the latest" by assumption; re-verify versionNumber + contentHash in the publish response). Accepted partial state: the QSP and LVA bumps are independent Jeff items — if LVA's publish fails after QSP is live, QSP stays live (correct on its own) and LVA is fixed via corrected draft; no all-or-nothing requirement, but the dry-run makes this path unlikely. Failure half-way: drafts are inert — a failed/wrong draft is superseded by a corrected re-seed, nothing user-visible until publish; publish is per-template atomic.

11. **Launch sequence (each ⚡ individually authorized).** Merge dark → ⚡ preflight (item 10 + **launch-integrity guards**: local git SHA matches the live Vercel deployment SHA; `DATABASE_URL` host fingerprint asserted against the known prod Neon host; seed output — exact new version ids + contentHashes — captured into the launch log BEFORE any publish) → ⚡ QSP body edit (item 8 — before re-seed, see item 10) → ⚡ re-seeds → verify drafts + publish dry-run both → ⚡ publish QSP vN+1 → ⚡ publish LVA vN+1 (the publish response doesn't echo hash/versionNumber, so **re-read each draft row immediately before AND after publish and abort on any hash drift** — closes the mutable-draft window between dry-run and publish) → ⚡ `WAVE_P_INVITE_EMAIL_ENABLED=1` + redeploy (straight global — user-decided in the grill over a canary stage; mitigations: flip in a quiet window after confirming **no imminent scheduled reminder/fan-out runs**, byte-identical flag-off proof, kill lever armed) → live smoke, immediately: new campaign pins new version + shows new labels; old campaign still shows old labels; real invite send inspected in a mail client (logo, button, layout, coach name in body); blank-name completion email shows address; watch send logs (`chromeVariant`/`logoIncluded`/`logoRejectedReason`) through the smoke window — any malformed rendering or send-failure spike = kill. Rollback: email = zero the flag + redeploy; **bad published version = publish corrected vN+2 AND enumerate campaigns created in the bad-version window** (query campaigns pinned to the bad versionId since its publishedAt — window is minutes and changes are label-only, but they stay pinned until handled; repin/retire per campaign before declaring rollback complete); bad body copy = re-edit the row (snapshot-first, per item 10 — note a post-publish body edit re-introduces hash skew, so the NEXT QSP re-seed will mint a follow-up draft; expected and documented, not silent churn). → Slack Jeff (terse): 5 shipped items + the #18 not-a-duplicate finding + #3 differentiate-not-delete rationale.

12. **SoT + tracking on push.** CLAUDE.md anchor + `plans/CHANGELOG.md` entry (freshness test), Notion task per house rules, spec doc `docs/specs/v7.6/18p-wave-p-quick-fixes.md` capturing the grilled decisions (incl. the ADR-0001 ruling and the CONTEXT.md invitation-copy fact already recorded).

## Out of scope
Jeff #2.3 revised email copy (TBD from Jeff — body edit keeps the token so it drops in later); #15 "The leadership" + #16 "Growth Financing" + #19 media question (waiting on Jeff/Suzanne); profileImage write-side URL validation (noted follow-on); P3–P8 roadmap tiers.

## Changelog

### Round 1 (Senior-engineer review) — all 8 findings accepted, 1 fact-checked against schema
- **H1 (coachName/logo identity mismatch) — TOOK, verified.** `resolveCoachName` falls back to `organization.owner`; my logo plan was creatorCoach-only → an admin-created campaign would show the owner's name with no/mismatched logo. Schema check: `Organization.ownerCoachId → Coach`, so the owner IS a Coach row with `profileImage`. Fix: logo resolution now mirrors `resolveCoachName` exactly — `(creatorCoach ?? organization.owner)` — name and logo always come from the same coach. Tests added for `creatorCoach=null, owner present` across all 4 send paths (item 7).
- **M1 (contentHash ordering) — TOOK.** The version `contentHash` is computed over the template row's invitation body at draft-creation time; editing the body AFTER publish would leave the published hash tied to the old body and mint a spurious draft on every future re-seed. Fix: the QSP body edit now runs BEFORE the QSP re-seed (items 8/10/11 reordered).
- **M2 (full custom-HTML invitations bypass the shell) — TOOK.** `renderFullHtmlBody` campaigns replace the entire shell, so "uniform chrome across all templates" over-claimed. Fix: full-HTML overrides explicitly EXCLUDED (coach authored the whole HTML) + a flag-ON-with-`invitationBodyHtml` unchanged-output test (item 6).
- **M3 (mailer contract gap) — TOOK.** Route selects alone could load `profileImage` and still drop it before the mailer. Fix: item 7 now names the full chain — `SendInvitesInput` / `InviteMailer` / `sendAssessmentInvitationEmail` — with tests asserting the MAILER receives `coachLogoUrl`.
- **M4 (http: images) — TOOK.** The email sanitizer already strips `http:` image sources; my http(s) gate was looser than the surrounding email code. Fix: email-specific HTTPS-only `safeImageSrc` (item 6). Kept the no-blob-allowlist decision (grilled with user): scheme-gate, not host-gate.
- **M5 (on-screen greeting leak) — TOOK.** The `respondent-report.ts` fallback feeds page renderers, not just email. Fix: centralized `displayName`/`greetingName` helper module used by the email builder AND `BrandedReport`/`QualitativeReport` greeting derivations; page-renderer tests added (item 4).
- **M6 (partial-publish state undefined) — TOOK.** Fix: dry-run `TemplateVersionForPublishSchema` against BOTH drafts before publishing EITHER; documented the accepted partial state (QSP and LVA bumps are independent Jeff items — QSP staying live while LVA gets a corrected draft is acceptable, not a corruption) (item 10).
- **L1 (S5 labels also change) — TOOK.** `FACTORS_FOR_MATRIX` also derives the S5 "Why is … a hindrance?" labels, so the sentence-casing propagates there — intended, now stated in the plan and covered in seed tests (item 2).
- **Rejected:** none. The H1 schema fact-check corrected MY assumption (owner is a Coach, not a User) — the finding stands and the fix is cleaner for it.

### Round 2 (Security & data-integrity review) — 5 findings: 4 taken, 1 taken-partially
- **M1 (remote-logo tracking/exfiltration channel) — TOOK PARTIALLY.** The finding is real: any https `profileImage` = a third-party image loaded on respondent email-open. NOT adopting the host-allowlist/proxy (the user explicitly chose scheme-gate over allowlist in the grill, and a proxy is out of quick-fix scope). Adopted instead: launch-preflight audit of every existing `Coach.profileImage` host before the flip, the risk recorded as accepted-with-audit (values written only by authenticated coaches/admins; Wave K already renders the same images on web report pages), and proxy/allowlist named as the follow-on (item 6, item 10a).
- **M2 (body edit has no CAS; crash/concurrent-edit window) — TOOK.** Procedural CAS: snapshot old body (rollback value) → edit → re-read and verify verbatim → seed → verify the draft's hash inputs include the final body (item 10). A concurrent admin edit between verify and seed remains theoretically possible but the launch is single-operator and the re-read closes the realistic window.
- **M3 (alt-text injection via coach name) — TOOK.** `alt` is user-controlled: escape + control-strip with the same helpers as every insertion; tests with quotes/angle brackets/control chars (item 6).
- **L1 ("latest is published" ≠ "no drafts"; publish route accepts any draft) — TOOK.** Preflight now asserts zero `publishedAt IS NULL` rows; the publish step targets the recorded draft id explicitly and re-verifies versionNumber + contentHash (item 10).
- **L2 (no logo observability) — TOOK.** PII-free `chromeVariant`/`logoIncluded`/`logoRejectedReason` logged per send, never the URL (item 7).

### Round 3 (Ops & SRE review) — 6 findings: 4 taken, 1 taken-partially, 1 rejected (user decision)
- **H1 (bad-version rollback leaves pinned campaigns) — TOOK.** Publishing corrected vN+2 only fixes future campaigns; ones created in the bad-version window stay pinned. Rollback now enumerates campaigns pinned to the bad versionId since its publishedAt and handles each (repin/retire) before rollback is declared complete (item 11). The window is minutes and Wave P changes are label-only, so exposure is bounded — but the enumeration step makes it explicit rather than assumed.
- **H2 (straight-global vs canary) — REJECTED (user decision), mitigation taken.** The canary-vs-global choice was put to the user verbatim in the grill with this exact tradeoff, and they chose straight global + live smoke; not re-litigating. Adopted the exposure-awareness half: flip in a quiet window after confirming no imminent scheduled reminder/fan-out runs, watch send telemetry through the smoke, kill lever armed (item 11). The byte-identical flag-off proof + logo-absent graceful path bound the blast radius of the worst realistic failure to cosmetic.
- **M1 (publish response lacks hash echo; mutable-draft window) — TOOK (procedurally).** Re-read each draft row immediately before AND after publish, abort on hash drift (item 11). Chose the procedural guard over adding an expectedContentHash parameter to the publish route — a route change is beyond quick-fix scope and the launch is single-operator.
- **M2 (no deploy-SHA/DB-fingerprint/seed-manifest guards) — TOOK.** Preflight now asserts local SHA == deployed SHA, fingerprints the DB host, and captures seed output (version ids + hashes) into the launch log before any publish (item 11).
- **M3 (no metrics/alert thresholds) — TOOK PARTIALLY.** Chrome fields ride the existing send telemetry and the smoke window has an explicit kill trigger (malformed rendering / send-failure spike). Full dashboards/alert wiring stays in the P8 hardening tier — same accepted posture as the Wave O launch (human-read logs + kill switch), recorded as a follow-on rather than a Wave P deliverable.
- **L1 (body rollback re-creates hash skew) — TOOK.** Documented: a post-publish body re-edit makes the published hash stale again, so the next QSP re-seed mints a follow-up draft — expected, snapshot-first, not silent churn (item 11).
